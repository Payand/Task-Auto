export enum TokenEnum {
  Bearer = "Bearer",
  InvalidToken = "INVALID_TOKEN",
  InvalidRefreshToken = "INVALID_REFRESH_TOKEN",
  AccessTokenExpired = "ACCESS_TOKEN_EXPIRED",
  RefreshTokenExpired = "REFRESH_TOKEN__EXPIRED",
}
